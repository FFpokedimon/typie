/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./.{html,js,ts,jsx,tsx}"
    ],
    theme: {
        extend: {
            // Customizations like new colors, font families, etc.
        },
    },
    plugins: [],
};
